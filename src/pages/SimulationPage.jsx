import { PagePlaceholder } from '../components/PagePlaceholder'

/** M2: kolam 4 zona, perenang draggable, urutan Meronta→Diam→Alarm + buzzer. */
export function SimulationPage() {
  return <PagePlaceholder pageId="simulation" />
}
