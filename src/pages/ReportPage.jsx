import { PagePlaceholder } from '../components/PagePlaceholder'

/** M4: tabel alarm, grafik bulanan, statistik respons, heatmap zona. */
export function ReportPage() {
  return <PagePlaceholder pageId="report" />
}
