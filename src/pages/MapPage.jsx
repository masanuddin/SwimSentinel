import { PagePlaceholder } from '../components/PagePlaceholder'

/** M3: kolam tampak atas 4 zona, status live, kartu korban + Tanggapi. */
export function MapPage() {
  return <PagePlaceholder pageId="map" />
}
